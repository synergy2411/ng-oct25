import { CoountryCodePipe } from './coountry-code.pipe';

describe('CoountryCodePipe', () => {
  it('create an instance', () => {
    const pipe = new CoountryCodePipe();
    expect(pipe).toBeTruthy();
  });
});
